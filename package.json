{
  "name": "nf-dashboard-ai-proxy",
  "version": "1.0.0",
  "description": "Proxy seguro que guarda a chave da Anthropic no servidor para o painel de Notas Fiscais",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
